services:
  frontend:
    image: my-frontend
    build: ./frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true # Enable live reloading
    depends_on:
      - backend

  backend:
    image: my-backend
    build: ./backend
    ports:
      - "5001:5001"
    environment:
      - MONGO_URI=mongodb+srv://jeffreyye:0zl43zA9XE66kDZE@cluster0.qpdiy.mongodb.net/wordleplus?retryWrites=true&w=majority&appName=Cluster0